// Import all images here so they are loaded once at startup
import Logo from "@divaastra/assets/images/logo.png";

// Export as a single object
const IMAGES = {
  logo: Logo
};

export default IMAGES;
